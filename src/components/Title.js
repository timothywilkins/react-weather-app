import React from 'react';

const Title = props => (
    <div>
        <h1 className="title-container__title">React Weather API</h1>
        <p className="title-container__subtitle">Powered by React and Bootsrap</p>
    </div>
)

export default Title;